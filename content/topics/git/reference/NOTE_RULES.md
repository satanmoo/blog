# Git reference 노트 작성 규칙

이 문서는 `topics/git/reference/` 아래 Git 공식 reference 노트의 추가 규칙임

- `tags`는 `reference`, `git` 두 개만 사용함
- `aliases`는 문서의 H1 제목과 동일하게 둠
- 공식 문서 링크는 필요하면 frontmatter의 `references`에 기록함
